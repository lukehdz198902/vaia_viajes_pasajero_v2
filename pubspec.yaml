name: vaia_viajes_pasajero_v2
description: "Vaia Viajes - App de Pasajero"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.2
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^9.2.3
  google_maps_flutter: ^2.10.0
  geolocator: ^13.0.2
  intl: ^0.20.2
  url_launcher: ^6.3.1
  signalr_netcore: ^1.3.9
  firebase_core: ^4.15.0
  firebase_messaging: ^16.7.0
  google_sign_in: ^7.2.0
  image_picker: ^1.2.3
  permission_handler: ^13.0.2
  webview_flutter: ^4.14.1
  local_auth: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
